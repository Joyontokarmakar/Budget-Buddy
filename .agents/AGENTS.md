# Workspace Rules

## Commit Messages
- Never use Conventional Commits prefixes (e.g., "feat:", "fix:", "chore:", "docs:") in git commit messages. Commit messages should be in plain text and start directly with the description.
